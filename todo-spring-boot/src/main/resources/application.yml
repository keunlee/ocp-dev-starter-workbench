spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: "jdbc:postgresql://${DATABASE_SERVICE_HOST}:5432/${DATABASE_DB_NAME}"
    username: ${DATABASE_DB_USER}
    password: ${DATABASE_DB_PASSWORD}